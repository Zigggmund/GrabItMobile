import { View } from 'react-native';

import { CustomText } from '@/components/ui/text/CustomText';

export default function RegistrationPage() {
  return (
    <View>
      <CustomText className={'text-50'} highlight>
        Registration
      </CustomText>
    </View>
  );
}
